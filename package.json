{
  "name": "ofek-race",
  "version": "1.0.0",
  "private": true,
  "description": "Ofek's racing game for web and Android.",
  "scripts": {
    "start": "node server.js",
    "prepare:web": "node scripts/prepare-web.js",
    "android:sync": "npm run prepare:web && npx cap sync android",
    "android:build": "cd android && gradlew.bat assembleDebug",
    "android:bundle": "npm run android:sync && cd android && gradlew.bat bundleRelease"
  },
  "dependencies": {
    "@capacitor-community/admob": "^7.0.3",
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
